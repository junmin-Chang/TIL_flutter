Exception Handling
try {

 some codes to try
}

catch (e) {
 print(e)
 if codes in {try block} , print(e) in {catch block} excutes
}


Null Aware Operators

myNumber = null;

margin: EdgeInsets.all(myNumber ?? 30),


?? --> if myNumber is null, turn myNumber into 30
